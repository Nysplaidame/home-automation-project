---
title: "GL.iNet GL-MT6000 (Router)"
category: entity
tags: [hardware, network, router, openwrt, vlan]
created: 2026-04-07
updated: 2026-09-04
sources: [project-readme, network-architecture-decision, hardware-bom, router-setup-complete]
status: stable
---

# GL.iNet GL-MT6000

**Type:** device — router / firewall
**Status:** ✅ Owned / ✅ Deployed and stable
**Related:** [[concepts/vlan-segmentation]], [[concepts/wireguard-vpn]], [[concepts/tailscale-remote-access]], [[entities/home-assistant]], [[entities/ventsys]], [[entities/monitoring-vm]], [[entities/docker-host]], [[entities/adguard-home]]

## Overview

WiFi 6 router running OpenWrt (DSA), serving as the network core for the home automation system. It implements the 11-VLAN security-segmented architecture. Router configs are deployed via `tools/router-deploy`; the router is stable on management IP `192.168.10.1`.

The owner confirmed Zen internet working directly through the Openreach ONT
on 2026-09-04. Logical interface `wan` uses physical Ethernet device `eth1`
with PPPoE and no WAN VLAN tag. Source now matches this WAN and adds cloud
IoT VLAN55; deployment requires locally injected PPPoE credentials.

## Key Properties

2026-09-04 repair acceptance: all 48 local DNS aliases pass; the retired Zyxel
station is disabled and all five 2.4 GHz APs are up. WAN6 now uses `@wan`, but
Zen replies NoAddrsAvail / NoPrefixAvail; provider allocation remains open.
Hive is reserved at `192.168.55.10`, with DHCP, internet TCP443 and router NTP
traffic verified. Cloud forwarding allows WAN only; router input allows
DHCP/DNS/NTP only. Runtime ruleset validation passes. The owner still sees
Hive offline in the app, so application/account acceptance remains open.
See [[../../../main/HANDOFF-2026-07-27-portal-services]].

- Platform: vanilla OpenWrt **24.10.3**, kernel **6.6.104** (authenticated 2026-09-04)
- Management IP: `192.168.10.1` (VLAN 10)
- Workstation SSH: `home-router-lan`, dedicated Ed25519 key installed and
  passwordless root login verified; private key remains outside the vault.
- HomeAdmin: live auto channel selected **100**, **HE80**, country **GB**;
  PC signal approximately **-76 dBm**, observed download PHY **432 Mbps**.
  Source/measurement limits and firmware upgrade candidates are recorded in
  [[../../../main/HANDOFF-2026-07-27-portal-services]].
- Ports: 5× GbE LAN + 1× WAN
- WiFi: WiFi 6 (802.11ax), dual-band
- WireGuard: built-in support (VLAN 70 / DMZ)
- Deploy tool: `tools/router-deploy/` (first-flight applied, full profile pending)

## Port Assignments

| Port | Role | VLAN |
|---|---|---|
| WAN (`eth1`; logical `wan`) | Openreach ONT / Zen PPPoE | No WAN VLAN tag |
| lan1 | Trunk → MINISFORUM Proxmox | Tagged: 10,20,30,35,40,50,60,70 |
| lan2 | Hive hub on `cloud_iot` | VLAN 55 untagged; WAN only; no internal forwarding |
| lan3 | GS1900 switch trunk | Tagged: 1,10,30,40 — cameras on ports 2-4; port 8 spare storage access |
| lan4 | OMV NAS direct connection | VLAN 40 untagged — owner confirmed 2026-09-04 |
| lan5 | LAN / recovery | VLAN 1 untagged — user/recovery access |

## VLAN Architecture (11 segments)

Cloud IoT VLAN55 was added for Hive on LAN2, separate from the internet-denied
safety VLAN50. Router DNS uses AdGuard with public fallback; standard external
DNS ports 53/853 are blocked, though this does not prevent every DNS bypass.

| VLAN | Name | Subnet | Internet |
|---|---|---|---|
| 1 | LAN (users) | 192.168.1.0/24 | ✅ |
| 10 | Management | 192.168.10.0/24 | ✅ |
| 20 | Automation (HA) | 192.168.20.0/24 | HA only |
| 30 | NVR (Frigate) | 192.168.30.0/24 | ❌ |
| 35 | Printers | 192.168.35.0/24 | OTA only |
| 40 | Storage (NAS) | 192.168.40.0/24 | ❌ |
| 50 | IoT sensors | 192.168.50.0/24 | ❌ |
| 55 | Cloud IoT (Hive) | 192.168.55.0/24 | WAN only |
| 60 | Monitoring | 192.168.60.0/24 | Limited |
| 70 | DMZ / VPN | 192.168.70.0/24 | Limited |
| 99 | Guest | 192.168.99.0/24 | ✅ |

## WiFi SSIDs

- **HomeMain** — VLAN 1 (main users)
- **HomePrinters** — VLAN 35 (printer OTA/LAN)
- **HomeIoT** — VLAN 50 (2.4GHz, client isolation)
- **HomeGuest** — VLAN 99
- Plus management/monitoring SSIDs as defined in `wireless-config.conf`.

## Config Files

All canonical files in `main/configs/openwrt/`:
- `vlan-config.conf` — VLANs 1/10/20/30/35/40/50/60/70/99.
- `firewall-config.conf` — zones and inter-VLAN policy.
- `dhcp-config.conf` — scopes and static reservations.
- `wireless-config.conf` — SSIDs and radio settings.

Deployment toolkit: `tools/router-deploy/` (lint, compile, deploy, test scripts).

## Live Firewall / Policy Notes

- Temporary rule `TEMP Docker Host Update Access` was removed after Docker/Bambuddy pulls and apt-cache validation.
- `apt-cacher-ng` on docker-host is reachable from `frigate-nvr` via permanent `Frigate to APT Cache` rule.
- Router DNS targets [[entities/adguard-home]] on docker-host first, with Quad9/Cloudflare public fallback.
- docker-host has narrow permanent egress for Tailscale, AdGuard and Mullvad
  WireGuard. The host-specific `Docker Host VPN Egress` rule includes UDP
  `51820`; general Docker pulls remain maintenance-window actions.
- Router-local NTP server is enabled, with UDP/123 input rules for restricted VLANs that need local time.
- OpenWrt forwards syslog to monitoring VM (`192.168.60.10:514/udp`).
- Router/source parity has been cleaned up: no valve-specific plain-MQTT `1883` exception is treated as current. VentSys MQTT traffic should use TLS `8883`; any plaintext bootstrap exception must be explicitly temporary and documented before use.
- The canonical 48-entry `home.local` domain inventory is live in dnsmasq and passed full source/service-matrix/router resolution validation on 2026-07-29.

## Validation Snapshot

- `test.ps1 -RouterIp 192.168.10.1 -Profile first-flight`: `PASS=62 WARN=0 FAIL=0`.
- `test-connectivity.ps1 -RouterIp 192.168.10.1 -Profile first-flight`: `PASS=74 WARN=0 FAIL=0`.
- Core health baseline: `health_check.sh --json` summary `PASS=11 FAIL=0` with hardware/future services parked unless `--full` is passed.

## Open Questions

- [ ] Deploy full profile when WiFi passwords/WireGuard keys are ready.
- [ ] Fill MAC address placeholders in `dhcp-config.conf` as hardware arrives.
- [ ] Keep WireGuard as fallback; use Tailscale for daily remote access.
- [ ] Reconcile deployment source with working Zen PPPoE on `eth1`; verify
  IPv6 and retire the temporary Wi-Fi uplink after its impact is checked.

## Change Log

- 2026-09-04: Verified vanilla OpenWrt 24.10.3 and dedicated workstation SSH
  key access. Recorded measured HomeAdmin link limitations and later observed
  2.4 GHz recovery/Hive VLAN 55, with acceptance boundaries in the handoff.

- 2026-09-04: Owner confirmed NAS on router LAN4, superseding the incorrect
  August port-8 map. Recorded pending Hive LAN2 research, with no live changes.

- 2026-08-25: Corrected the physical port map to the live GS1900 trunk, OMV
  port 8 path and unplugged VLAN 40 recovery port on `lan4`.
- 2026-08-01: Expanded the existing docker-host VPN egress rule with UDP 51820
  for Mullvad WireGuard without allowing general Automation-zone internet.
- 2026-07-29: Deployed and validated all 48 canonical `home.local` aliases.
- 2026-06-29: Synced port assignments to the managed-switch topology and removed the stale valve-1 plain-MQTT firewall exception as a current open issue.
- 2026-05-23: Added AdGuard-first DNS, Tailscale daily access direction, and WireGuard fallback framing.
- 2026-05-18: Updated validation counts, monitoring/syslog state, and live-only valve-1 firewall exception risk.
- 2026-05-08: Updated to deployed/stable; 11-VLAN table, port validation, post-deployment cleanup notes.
- 2026-04-07: Page created from README + network architecture decision ingest.
