# Prefer a SHA-256 hash for the local admin password.
# Generate on OMV:
#   python3 - <<'PY'
#   import getpass, hashlib
#   print(hashlib.sha256(getpass.getpass().encode()).hexdigest())
#   PY
TRANSFERPORTAL_ADMIN_USER=admin
TRANSFERPORTAL_ADMIN_PASSWORD_SHA256=replace-with-sha256

