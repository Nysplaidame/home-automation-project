# AdGuard Home Template

This template contains the Compose wiring only.

Do not commit the live `conf/AdGuardHome.yaml`; it contains installation state
and the admin password hash. Use the install manual to complete first-run setup,
then export/backup the live config through the operational backup path.
