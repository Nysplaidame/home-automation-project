name: calibre-web

services:
  calibre-web:
    image: lscr.io/linuxserver/calibre-web:version-0.6.26@sha256:7c0464228f2fdcf543b46e5327f36114596b097db6af0297c26b0452ed15f17c
    container_name: calibre-web
    restart: unless-stopped
    ports:
      - "8083:8083"
    environment:
      PUID: "${MEDIA_UID}"
      PGID: "${MEDIA_GID}"
      TZ: Europe/London
    volumes:
      - ./config:/config
      - /mnt/omv/media/books/calibre-library:/books
    security_opt:
      - no-new-privileges:true

networks:
  default:
    name: calibre-web
    ipam:
      config:
        - subnet: 10.240.11.0/24
