# Immich Staged Skeleton

This stack is deployed for pre-flight only. `UPLOAD_LOCATION=./library` is local
placeholder storage on VM 103. Do not import a real photo library until
OMV-backed storage is mounted and backup/restore steps are documented.

Current live note: `immich_machine_learning` is intentionally stopped to keep
VM 103 stable at the current pre-flight sizing.
