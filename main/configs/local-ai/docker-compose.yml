# Local AI LXC — Docker Compose
# Host: CT 114 / llm-host (192.168.20.104, VLAN 20)
# Deploy to: /opt/stacks/local-ai/docker-compose.yml

services:
  llama-cpp:
    image: ghcr.io/ggml-org/llama.cpp:server-vulkan
    container_name: llama-cpp
    restart: unless-stopped
    ports:
      - "8081:8080"
    volumes:
      - ./models:/models:ro
    devices:
      - /dev/dri/renderD128:/dev/dri/renderD128
      - /dev/dri/card0:/dev/dri/card0
    command:
      - --model
      - /models/home-assistant-llm.gguf
      - --alias
      - home-assistant-llm
      - --host
      - 0.0.0.0
      - --port
      - "8080"
      - --ctx-size
      - "8192"
      - --threads
      - "4"
      - --parallel
      - "1"
      - --n-gpu-layers
      - "99"
      - --jinja

  llama-cpp-embeddings:
    image: ghcr.io/ggml-org/llama.cpp:server-vulkan
    container_name: llama-cpp-embeddings
    restart: unless-stopped
    ports:
      - "8082:8080"
    volumes:
      - ./models:/models:ro
    devices:
      - /dev/dri/renderD128:/dev/dri/renderD128
      - /dev/dri/card0:/dev/dri/card0
    command:
      - --model
      - /models/bge-small-en-v1.5-q8_0.gguf
      - --alias
      - home-assistant-embedding
      - --host
      - 0.0.0.0
      - --port
      - "8080"
      - --ctx-size
      - "512"
      - --threads
      - "2"
      - --threads-http
      - "2"
      - --parallel
      - "1"
      - --batch-size
      - "128"
      - --ubatch-size
      - "128"
      - --n-gpu-layers
      - "0"
      - --embedding
      - --pooling
      - mean

  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: open-webui
    restart: unless-stopped
    depends_on:
      - llama-cpp
    ports:
      - "3002:8080"
    environment:
      - OPENAI_API_BASE_URL=http://llama-cpp:8080/v1
      - OPENAI_API_KEY=sk-no-key-required
      - ENABLE_RAG_WEB_SEARCH=true
      - RAG_WEB_SEARCH_ENGINE=searxng
      - SEARXNG_QUERY_URL=http://192.168.20.102:8087/search?q=<query>&format=json
      - RAG_WEB_SEARCH_RESULT_COUNT=3
      - RAG_WEB_SEARCH_CONCURRENT_REQUESTS=10
    volumes:
      - ./open-webui:/app/backend/data

  wyoming-whisper:
    image: rhasspy/wyoming-whisper:latest
    container_name: wyoming-whisper
    restart: unless-stopped
    ports:
      - "10300:10300"
    command: ["--model", "base-int8", "--language", "en", "--debug"]
    environment:
      # VLAN 20 has no internet egress. Keep the tokenizer cache under /data
      # and force deterministic offline startup.
      - HF_HOME=/data/huggingface
      - HF_HUB_OFFLINE=1
    volumes:
      - ./whisper:/data

  wyoming-piper:
    image: rhasspy/wyoming-piper:latest
    container_name: wyoming-piper
    restart: unless-stopped
    ports:
      - "10200:10200"
    command:
      [
        "--voice",
        "en_GB-alba-medium",
        "--auto-punctuation",
        ".",
        "--debug",
      ]
    volumes:
      - ./piper:/data

  wyoming-openwakeword:
    image: rhasspy/wyoming-openwakeword:latest
    container_name: wyoming-openwakeword
    restart: unless-stopped
    ports:
      - "10400:10400"
    command: ["--debug"]
    volumes:
      - ./openwakeword:/custom
